
{
  "name": "node-test",
  "version": "1.0.0",
  "description": "creating my fisrt \"Hello-world!\"Node project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "diksha",
  "license": "ISC",
  "dependencies": {
    "left-pad": "^1.3.0"
  }
}
const leftPad = require('left-pad');         // Require left pad
const output = leftPad('Hello, World!', 15); // Define output

// Send output to the console
console.log(output);
